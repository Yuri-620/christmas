<svg xmlns="http://www.w3.org/2000/svg" width="800" height="600" viewBox="0 0 800 600">
  <!-- 背景渐变 -->
  <defs>
    <linearGradient id="bg" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" style="stop-color:#1a0000;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#4d0000;stop-opacity:1" />
    </linearGradient>

    <!-- 发光效果 -->
    <filter id="glow">
      <feGaussianBlur stdDeviation="4" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- 文字阴影 -->
    <filter id="shadow">
      <feDropShadow dx="0" dy="4" stdDeviation="6" flood-color="#000000" flood-opacity="0.8"/>
    </filter>
  </defs>

  <!-- 背景 -->
  <rect width="800" height="600" fill="url(#bg)"/>

  <!-- 装饰性的雪花 -->
  <circle cx="100" cy="80" r="3" fill="#ffffff" opacity="0.6"/>
  <circle cx="700" cy="120" r="2" fill="#ffffff" opacity="0.5"/>
  <circle cx="150" cy="500" r="2.5" fill="#ffffff" opacity="0.7"/>
  <circle cx="680" cy="480" r="3" fill="#ffffff" opacity="0.6"/>
  <circle cx="250" cy="150" r="2" fill="#ffffff" opacity="0.5"/>
  <circle cx="600" cy="200" r="2.5" fill="#ffffff" opacity="0.6"/>

  <!-- 装饰性的星星 -->
  <text x="120" y="200" font-size="40" fill="#ffd700" opacity="0.8" filter="url(#glow)">✨</text>
  <text x="650" y="350" font-size="35" fill="#ffd700" opacity="0.7" filter="url(#glow)">⭐</text>
  <text x="180" y="450" font-size="30" fill="#ffd700" opacity="0.6" filter="url(#glow)">✨</text>

  <!-- 主文字 -->
  <text x="400" y="250"
        font-family="'PingFang SC', 'Microsoft YaHei', 'SimHei', sans-serif"
        font-size="56"
        font-weight="bold"
        fill="#ffd700"
        text-anchor="middle"
        filter="url(#shadow)">宝宝大人圣诞节快乐</text>

  <text x="400" y="340"
        font-family="'PingFang SC', 'Microsoft YaHei', 'SimHei', sans-serif"
        font-size="68"
        font-weight="bold"
        fill="#ff6b6b"
        text-anchor="middle"
        filter="url(#shadow)">我爱你</text>

  <!-- 装饰性的心形 -->
  <text x="400" y="440" font-size="50" text-anchor="middle" filter="url(#glow)">💝</text>

  <!-- 底部装饰 -->
  <text x="400" y="520"
        font-family="'PingFang SC', 'Microsoft YaHei', sans-serif"
        font-size="24"
        fill="#d4af37"
        text-anchor="middle"
        opacity="0.9">🎄 Merry Christmas 🎄</text>
</svg>
